"use strict"; // treats all the JS Code as newer version

let age = 19
let name = "Ansh Darji"

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// datatype of null is object / null => standalone value
// undefined is one type of datatype itself
// symbol => unique

console.log(typeof age) // number
console.log(typeof name) // string
console.log(typeof null) // object
console.log(typeof undefined) // undefined

/*
ECMA (ECMAScript) is the official standard that defines how JavaScript works. It ensures all
browsers follow the same rules. Features like let, const, and arrow functions come from
ECMAScript updates.
*/